#define _ASM_S390_BUG_H
#define __EMIT_BUG(x) do  while (0)
#define __EMIT_BUG(x) do  while (0)
#define BUG() do  while (0)
#define __WARN_TAINT(taint) do  while (0)
#define WARN_ON(x) ()
#define HAVE_ARCH_BUG
#define HAVE_ARCH_WARN_ON
