#define F_HAVE_BREAKS   1
#define F_HAVE_AFFINITY 2
#define BREAK_LEFT  1
#define BREAK_RIGHT 2
OFFSET offsetof(PullupContext, x)
#define FLAGS AV_OPT_FLAG_VIDEO_PARAM|AV_OPT_FLAG_FILTERING_PARAM
static const AVOption pullup_options[] = ;
AVFILTER_DEFINE_CLASS(pullup);
query_formats
ABS (((a) ^ ((a) >> 31)) - ((a) >> 31))
diff_c
comb_c
var_c
alloc_metrics
free_field_queue
*make_field_queue
config_input
*pullup_lock_buffer
pullup_release_buffer
alloc_buffer
*pullup_get_buffer
queue_length
find_first_break
compute_breaks
compute_affinity
decide_frame_length
*pullup_get_frame
pullup_release_frame
compute_metric(const uint8_t *, const uint8_t *, ptrdiff_t))
check_field_queue
pullup_submit_field
copy_field
pullup_pack_frame
filter_frame
uninit
static const AVFilterPad pullup_inputs[] = ;
static const AVFilterPad pullup_outputs[] = ;
AVFilter ff_vf_pullup = ;
