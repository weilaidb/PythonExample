html_color_parse
enum ;
typedef struct SrtStack  SrtStack;
rstrip_spaces_buf
ff_htmlmarkup_to_ass
{
char *param, buffer[128], tmp[128];
int len, tag_close, sptr = 1, line_start = 1, an = 0, end = 0;
SrtStack stack[16];
stack[0].tag[0] = 0;
strcpy(stack[0].param[PARAM_SIZE],  );
strcpy(stack[0].param[PARAM_COLOR], );
strcpy(stack[0].param[PARAM_FACE],  );
for (; !end && *in; in++)
