#define AVFFT 0
#if HAVE_UNISTD_H
#if AVFFT
#if FFT_FLOAT
MUL16 ((a) * (b))
CMAC          \
#if FFT_FLOAT || AVFFT
#define RANGE 1.0
REF_SCALE  (x)
#define FMT
#elif FFT_FIXED_32
#define RANGE 8388608
REF_SCALE (x)
#define FMT
#define RANGE 16384
REF_SCALE ((x) / (1 << (bits)))
#define FMT
static struct  *exptab;
fft_ref_init
fft_ref
#if CONFIG_MDCT
imdct_ref
mdct_ref
#if FFT_FLOAT
#if CONFIG_DCT
idct_ref
dct_ref
frandom
check_diff
fft_init
mdct_init
mdct_calc
imdct_calc
fft_permute
fft_calc
mdct_end
fft_end
#if FFT_FLOAT
rdft_init
dct_init
rdft_calc
dct_calc
rdft_end
dct_end
help
enum tf_transform ;
#if !HAVE_GETOPT
main
