enum BMVFlags;
#define SCREEN_WIDE 640
#define SCREEN_HIGH 429
typedef struct BMVDecContext  BMVDecContext;
NEXT_BYTE (v) = forward ? (v) + 1 : (v) - 1;
decode_bmv_frame
decode_frame
decode_init
AVCodec ff_bmv_video_decoder = ;
