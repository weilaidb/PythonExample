#define KBUF_LEN 12
#define	INIT_KEY(key, config) do  while (0)
#define	GET_KEY_NEXT(key, config, kbuf, i) do  while (0)
#undef	M_32_SWAP
#define	M_32_SWAP(a)
typedef struct bench_qentry bench_qentry;
typedef struct bench_q  bench_q;
#define	BENCH_Q_TAIL_INSERT(queue, buf) do  while (0)
#define	BENCH_Q_POP(queue, buf) do  while (0)
#define	BENCH_Q_POP_PUSH(queue, buf) do  while (0)
typedef enum  test_type;
typedef struct
CONFIG;
